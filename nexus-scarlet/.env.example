NODE_ENV=development
PORT=4000
DATABASE_URL=postgres://nexus:nexus@localhost:5432/nexus
CORS_ORIGIN=http://localhost:5173

# Cipher AI Provider Configuration (defaults to deterministic fallback if unconfigured or unavailable)
AI_PROVIDER=deterministic
AI_MODEL=v1-deterministic
